Generative discriminator networks are popular in generating images especially in fields where data are hard to be collected.

- GAN, original one

- Conditional GAN, used for image to image translation
